<% if user_signed_in? %>
  <div class="user-info" onclick="this.style.display='none'">
    <p>Signed in under <b><%= current_user.username %></b>.</p>
  </div>
<% end %>
<div id="main_container">
  <%= image_tag("ajax-loader.gif") %>
  <h1>Loading may take a while...</h1>
</div>